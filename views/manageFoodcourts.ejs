<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Manage Foodcourts</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
</head>
<body>
    <nav class="navbar navbar-expand-sm navbar-dark bg-dark">
        <div class="container">
            <a class="navbar-brand" href="/">Food Availability App</a>
            <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#collapsibleNavbar">
                <span class="navbar-toggler-icon"></span>
            </button>
            <div class="collapse navbar-collapse" id="collapsibleNavbar">
                <ul class="navbar-nav ms-auto">
                    <li class="nav-item">
                        <a class="nav-link" href="/addFoodcourt">Add New Foodcourt</a>
                    </li>
                </ul>
            </div>
        </div>
    </nav>

    <div class="container mt-4">
        <div class="text-center">
            <h2>Manage Foodcourts</h2>
        </div>

        <div class="my-4">
            <h4>Existing Foodcourts</h4>
            <table class="table table-hover">
                <thead>
                    <tr>
                        <th scope="col">Foodcourt ID</th>
                        <th scope="col">Foodcourt Image</th>
                        <th scope="col">Foodcourt Name</th>
                        <th scope="col">Location</th>
                        <th scope="col">Actions</th>
                    </tr>
                </thead>
                <tbody>
                    <% for(let i=0; i < foodcourts.length; i++) { %>
                        <tr>
                            <td><%= foodcourts[i].foodcourtID %></td>
                            <td><img src="/images/<%= foodcourts[i].image %>" class="img-fluid" style="max-width: 100px;"></td>
                            <td><%= foodcourts[i].name %></td>
                            <td><%= foodcourts[i].location %></td>
                            <td>
                                <a href="/editFoodcourt/<%= foodcourts[i].foodcourtID %>" class="btn btn-sm btn-primary me-2">Edit</a>
                                <a href="/deleteFoodcourt/<%= foodcourts[i].foodcourtID %>" onclick="return confirm('Are you sure you want to delete this foodcourt?')" class="btn btn-sm btn-danger">Delete</a>
                            </td>
                        </tr>
                    <% } %>
                </tbody>
            </table>
        </div>
    </div>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>
