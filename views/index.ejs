<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>
    <title>Food Availability App</title>
    <style>
    </style>
</head>
<body>
    <nav class="navbar navbar-expand-sm bg-dark navbar-dark">
        <div class="container-fluid">
            <a class="navbar-brand" href="/">Food Availability App</a>
            <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#collapsibleNavbar">
                <span class="navbar-toggler-icon"></span>
            </button>
            <div class="collapse navbar-collapse" id="collapsibleNavbar">
                <ul class="navbar-nav me-auto">
                    <li class="nav-item">
                        <a class="nav-link" href="/manageStalls">Manage Stalls</a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link" href="/manageFoodcourts">Manage Food Court</a>
                    </li>
                </ul>
            </div>
        </div>
    </nav>

    <div class="container mt-4">
        <div class="text-center">
            <h2>Food Courts</h2>
        </div>

        <div class="table-responsive mt-4">
            <table class="table table-hover text-center">
                <thead class="table-dark">
                    <tr>
                        <th scope="col" width="30%">Food Court Name</th>
                        <th scope="col" width="30%">Image</th>
                        <th scope="col" width="40%">Location</th>
                    </tr>
                </thead>
                <tbody>
                    <% for(let i=0; i < foodcourts.length; i++) { %>
                        <tr>
                            <td><a href="/foodcourt/<%= foodcourts[i].foodcourtID %>" class="btn btn-primary btn-sm"><%= foodcourts[i].name %></a></td>
                            <td><img src="/images/<%= foodcourts[i].image %>" class="img-fluid" style="max-width: 100px;" alt="<%= foodcourts[i].name %>"></td>
                            <td><%= foodcourts[i].location %></td>
                        </tr>
                    <% } %>
                </tbody>
            </table>
        </div>
    </div>

    <footer class="footer mt-auto py-3 bg-light">
        <div class="container text-center">
            <span class="text-muted">© 2024 Food Availability App. All rights reserved.</span>
        </div>
    </footer>
</body>
</html>
