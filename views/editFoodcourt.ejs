<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Edit Foodcourt</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
    <style>
    </style>
</head>
<body>
    <nav class="navbar navbar-expand-sm navbar-dark bg-dark">
        <div class="container">
            <a class="navbar-brand" href="/">Food Availability App</a>
            <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#collapsibleNavbar">
                <span class="navbar-toggler-icon"></span>
            </button>
            <div class="collapse navbar-collapse" id="collapsibleNavbar">
                <ul class="navbar-nav ms-auto">
                    <li class="nav-item">
                        <a class="nav-link" href="/manageFoodcourts">Back to Foodcourts</a>
                    </li>
                </ul>
            </div>
        </div>
    </nav>

    <div class="container">
        <div class="text-center">
            <h2>Edit Foodcourt</h2>
        </div>

        <form action="/editFoodcourt/<%= foodcourt.foodcourtID %>" method="POST" enctype="multipart/form-data">
            <div class="mb-3">
                <label for="foodcourtName" class="form-label">Foodcourt Name</label>
                <input type="text" class="form-control" id="foodcourtName" name="foodcourtName" value="<%= foodcourt.name %>" required>
            </div>
            <div class="mb-3">
                <label for="location" class="form-label">Location</label>
                <input type="text" class="form-control" id="location" name="location" value="<%= foodcourt.location %>" required>
            </div>
            <div class="mb-3">
                <label for="currentImage" class="form-label">Current Image:</label><br>
                <input type="hidden" id="currentImage" name="currentImage" value="<%= foodcourt.image %>">
                <img src="/images/<%= foodcourt.image %>" class="img-fluid image-preview mt-2" alt="Current Image">
            </div>
            <div class="mb-3">
                <label for="newImage" class="form-label">New Image:</label><br>
                <input type="file" id="newImage" name="newImage" accept="image/*" class="form-control">
            </div>
            <button type="submit" class="btn btn-primary">Update Foodcourt</button>
        </form>
    </div>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>
