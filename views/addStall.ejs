<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Add New Stall</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>
</head>
<body>
    <nav class="navbar navbar-expand-sm bg-dark navbar-dark">
        <div class="container-fluid">
            <a class="navbar-brand" href="/">Food Availability App</a>
            <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#collapsibleNavbar">
                <span class="navbar-toggler-icon"></span>
            </button>
            <div class="collapse navbar-collapse" id="collapsibleNavbar">
                <ul class="navbar-nav">
                    <li class="nav-item">
                        <a class="nav-link" href="/manageStalls">Back to Stalls</a>
                    </li>
                </ul>
            </div>
        </div>
    </nav>

    <div class="container mt-4">
        <div class="text-center"><h2>Add New Stall</h2></div>

        <div class="row mt-4">
            <div class="col-md-6">
                <form action="/addStall" method="POST" enctype="multipart/form-data">
                    <div class="mb-3">
                        <label for="sName" class="form-label">Stall Name:</label>
                        <input type="text" id="stallName" name="sName" class="form-control" required>
                    </div>
                
                    <div class="mb-3">
                        <label for="foodcourtID" class="form-label">Foodcourt ID:</label>
                        <select id="foodcourtID" name="foodcourtID" class="form-control" required>
                            <% foodcourts.forEach(foodcourt => { %>
                                <option value="<%= foodcourt.foodcourtID %>"><%= foodcourt.name %></option>
                            <% }) %>
                        </select>
                    </div>

                    <div class="mb-3">
                        <label for="image" class="form-label">Image:</label><br>
                        <input type="file" id="image" name="image" accept="image/*" class="form-control">
                    </div>
                
                    <button type="submit" class="btn btn-primary">Add Stall</button>
                </form>
            </div>
        </div>
    </div>
</body>
</html>
